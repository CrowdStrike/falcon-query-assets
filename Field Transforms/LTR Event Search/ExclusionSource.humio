/ Transform to convert ExclusionSource decimal to human-readable value.
| ExclusionSource match {
    1 => ExclusionSource := "PPDM_PROCESS_EXCLUSION" ;
    2 => ExclusionSource := "PPDM_TREE_EXCLUSION" ;
    4 => ExclusionSource := "LEGACY_SBDM_EXCLUSION" ;
    5 => ExclusionSource := "ALLOWED_BY_ENGINE" ;
    6 => ExclusionSource := "CUSTOMER_EXCLUSION" ;
    * => * }