// Transform to convert LdapSearchScope decimal to human-readable value.
| LdapSearchScope match {
    0 => LdapSearchScope := "BASE_OBJECT" ;
    1 => LdapSearchScope := "SINGLE_LEVEL" ;
    2 => LdapSearchScope := "WHOLE_SUBTREE" ;
    * => * }