// Transform to convert Protocol decimal to human-readable value.
| Protocol match {
    1  => Protocol := "ICMP" ;
    6  => Protocol := "TCP" ;
    17 => Protocol := "UDP" ;
    58 => Protocol := "ICMPv6" ;
    *  => * }