// Transform to convert TokenType decimal to human-readable value.
| TokenType match {
    0 => TokenType := "Invalid token value" ;
    1 => TokenType := "Primary token" ;
    2 => TokenType := "Impersonation token" ;
    * => * }